<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href = "views/style/answer.css">
    <title>마이페이지</title>
</head>
<body>
  <div id="all">
    <div id="header">
      <div id="top">
        <%- include('noLoginMenu') %>
        <a href="/home">
          <img src="views/image/logo.png" id="logo">
        </a>
      </div>
      <%- include('menu') %>
    </div>
    <div id="name">
      <h2>개찰 문의 등록</h2>
    </div>
    <div id="main">
      <div id="posts">
        <%
        for(var i = 0; i < result.length; i++){
            var data = result[i];
        %>
          <div id="post">
            <div id="t">
              <a href="/answer/<%= data.ask_id %>"><p id="title"><%= data.ask_title %></p></a>
              <input type="hidden" name="id" id="id" value="<%= data.id %>">
              <p id="count">(
                <%
                  if(data.ask_answer){
                %>
                답변 완료
                <%
              } else{
                %>
                답변 대기 중
                <% } %>
                )</p>
            </div>
            <div id="a">
              <p id="auth"><%= data.ask_auth %></p>
              <p id="date"><%= data.ask_date %></p>
            </div>
          </div>
        <% } %>
    </div>
    <div id="bottom">
      <hr>
      <br>
      <p>안산대학교 IT응용보안과 2B04 전자입찰시스템</p>
    </div>
  </div>
</body>
</html>
